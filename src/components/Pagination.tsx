import React from 'react'

interface PaginationProps {
    currentPage: number;
    totalPages: number;
}

const Pagination = () => {
  return (
    <div>
      
    </div>
  )
}

export default Pagination
